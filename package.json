{
  "name": "redux-flop",
  "description": "Wrap generator on prototype in a flo creator.",
  "repository": "git://github.com/joshrtay/redux-flop.git",
  "version": "0.1.1",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/foreach-obj": "^1.0.1",
    "redux-flo": "^1.0.5"
  },
  "devDependencies": {
    "@f/is-generator-object": "^1.3.0",
    "tape": "^4.2.2"
  }
}
